<h1>ARTICLES</h1>

<ul>
  <% @articles.each do |article| %>
    <li><strong><%= link_to article.title, article_path(article) %></strong>: <%= article.content %></li>
  <% end %>
</ul>

<%= link_to "Add a article",  new_article_path %>
